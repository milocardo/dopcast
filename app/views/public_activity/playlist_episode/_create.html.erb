<li class="list-group-item">
  <span class="glyphicon glyphicon-plus"></span>
  <small class="text-muted"><%= a.created_at.strftime('%H:%M:%S %-d %B %Y') %></small><br/>
  <% if a.trackable %>
    <%= image_tag a.trackable.episode, class: "img-feed" %>
    <%= link_to a.trackable.playlist.user.username, user_path(a.trackable.playlist.user) %> added <%= link_to a.trackable.episode.title, episode_path(a.trackable.episode) %> to the playlist <%= link_to a.trackable.playlist.name, playlist_path(a.trackable.playlist) %>
  <% else %>
    A podcast that is currently deleted was added.
  <% end %>
</li>
